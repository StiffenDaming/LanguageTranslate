# =========================================================
# 乡音工坊 - 完整依赖清单 (Python 3.10)
# 已验证所有功能（SVC 转换 + TTS）均可正常使用
# 版本锁定保证兼容性，推荐直接使用 pip 安装
# =========================================================

# ---------- 深度学习框架 ----------
torch==2.1.0
torchaudio==2.1.0

# ---------- 音频处理 ----------
librosa==0.11.0
soundfile==0.13.1
scipy==1.15.3
numpy==1.26.4
audioread==3.1.0               # librosa 依赖
pooch==1.8.2                    # librosa 依赖
praat-parselmouth==0.4.7        # 基频提取 (原 parselmouth)

# ---------- 特征提取与模型工具 ----------
transformers==4.30.2            # 用于 Hubert 等模型
tokenizers==0.13.3              # transformers 依赖
safetensors==0.7.0              # 模型权重加载
huggingface_hub==0.36.1         # 下载预训练模型
filelock==3.20.3                # 文件锁
fsspec==2026.1.0                # 文件系统抽象

# ---------- 配置与工具 ----------
pyyaml==6.0.3                   # 配置文件解析
omegaconf==2.0.6                # 结构化配置
hydra-core==1.0.7               # 可选，某些脚本使用
tqdm==4.67.1                    # 进度条
matplotlib==3.10.8              # 绘图（某些模块需要）

# ---------- 聚类与数值计算 ----------
scikit-learn==1.7.2
joblib==1.5.3                   # scikit-learn 依赖
threadpoolctl==3.6.0            # scikit-learn 依赖
faiss-cpu==1.13.2               # 特征检索

# ---------- 音频格式转换 ----------
ffmpy==1.0.0                    # 调用 ffmpeg 处理音频

# ---------- 网络请求 ----------
requests==2.32.5
urllib3==2.6.3
certifi==2026.1.4
charset-normalizer==3.4.4
idna==3.11

# ---------- TTS 服务 ----------
edge-tts==7.2.7                 # 微软 Edge TTS 在线合成

# ---------- 其他常用 ----------
packaging==25.0                 # 版本解析
regex==2026.1.15                # 正则（某些文本处理）
pillow==10.4.0                  # 图像处理（间接依赖）